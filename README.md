# ugly-studio-pg-builder
Public builder repo — free GitHub Actions quota to build the bundled Postgres +
pgvector binaries (esp. Intel-Mac darwin-x64, which needs an Intel runner) and
upload them to the R2 mirror. Same split-repo pattern as ugly-studio-win-builder.
